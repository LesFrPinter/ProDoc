<Window x:Class="ProDocEstimate.Views.MICR"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
        xmlns:local="clr-namespace:ProDocEstimate.Views" 
        d:DataContext="{d:DesignInstance Type=local:DetailFeatures}"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Background="LightGray"
        Title="MICR" Height="490" Width="800">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment"   Value="Bottom" />
            <Setter Property="Margin"              Value="2 0"    />
            <Setter Property="Padding"             Value="2 0"    />
        </Style>
        <Style TargetType="telerik:RadNumericUpDown">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin"   Value="4 0" />
            <Setter Property="Height"   Value="30" />
            <Setter Property="Width"    Value="60" />
            <Setter Property="NumberDecimalDigits" Value="0" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Minimum"  Value="0" />
        </Style>
    </Window.Resources>

    <Viewbox>

        <Grid Width="800" Height="470" VerticalAlignment="Top">

            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="*"  />
                <RowDefinition Height="*"  />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Label FontSize="22" Foreground="Red" Content="MICR" Grid.ColumnSpan="3" HorizontalAlignment="Center" Padding="2 0" Margin="0" VerticalAlignment="Center" Height="40" >
                <Label.BitmapEffect>
                    <DropShadowBitmapEffect ShadowDepth="5" Direction="260" Opacity="75" />
                </Label.BitmapEffect>
            </Label>

            <Label Grid.Row="1"                 Content="Digital"   Margin="0 6" />
            <Label Grid.Row="1" Grid.Column="1" Content="Pack2Pack" Margin="0 6" />
            <Label Grid.Row="1" Grid.Column="2" Content="Press"     Margin="0 6" />

            <telerik:RadNumericUpDown                 Grid.Row="2" Value="{Binding Digital}"   x:Name="M1" />
            <telerik:RadNumericUpDown Grid.Column="1" Grid.Row="2" Value="{Binding Pack2Pack}" x:Name="M2"  />
            <telerik:RadNumericUpDown Grid.Column="2" Grid.Row="2" Value="{Binding Press}"     x:Name="M3"/>

            <local:DetailFeatures x:Name="Details" Grid.Row="3" Grid.ColumnSpan="7" Height="120" VerticalAlignment="Top" Margin="0 10 0 0" QUOTENUM="{Binding Quote_Num}" CATEGORY="{Binding Category}" FTYPE="{Binding F_Type}" PRESSSIZE="{Binding Press_Size}" />

            <!--QUOTENUM="10001" FTYPE="DETAIL" CATEGORY="MICR"-->
            <!--<Label FontSize="22" Content="Material" VerticalAlignment="Top" HorizontalAlignment="Left" Foreground="Red" FontWeight="UltraBold" Margin="5 -2 0 0" Grid.RowSpan="3" Grid.Row="3" />-->

            <Grid Grid.Row="4" Height="180" Grid.ColumnSpan="3" Background="Yellow" VerticalAlignment="Top" Margin="0,-30,0,0" >
                <Label FontSize="22" Content="Labor" VerticalAlignment="Top" HorizontalAlignment="Left" Foreground="Red" FontWeight="UltraBold" />
            </Grid>

            <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.Column="4" HorizontalAlignment="Center" VerticalAlignment="Top" >
                <Button x:Name="btnSave"   Content="Save"   Padding="6 2" VerticalAlignment="Bottom" Click="btnSave_Click" />
                <Button x:Name="btnCancel" Content="Cancel" Padding="6 2" VerticalAlignment="Bottom" Click="btnCancel_Click" />
            </StackPanel>

        </Grid>

    </Viewbox>

</Window>